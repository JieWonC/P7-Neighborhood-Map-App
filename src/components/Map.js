import React from "react";

const Map = () => {
    return (
        <div id="map"
            tabIndex="-1"
            role="application"
            aria-label="map">
        </div>
    );
};

export default Map;